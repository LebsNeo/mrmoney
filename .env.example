# ─────────────────────────────────────────────────────────────────────────────
# MrMoney — Environment Variables
# Copy this file to .env.local and fill in your values.
# All variables marked REQUIRED will cause a startup error if missing/invalid.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database (Neon PostgreSQL) ─────────────────────────────────────────────
# REQUIRED — Get your connection string from https://neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@host.neon.tech/mrmoney?sslmode=require"

# ── NextAuth ───────────────────────────────────────────────────────────────
# REQUIRED — Generate a secure secret: openssl rand -base64 32
# Must be at least 32 characters
NEXTAUTH_SECRET="replace-with-at-least-32-character-secret-here!!"

# REQUIRED — The canonical URL of your app (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"

# ── App URLs ───────────────────────────────────────────────────────────────
# REQUIRED — Must match NEXTAUTH_URL in most deployments
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# OPTIONAL — Display name for the application
NEXT_PUBLIC_APP_NAME="MrMoney"

# ── Production (Vercel) ────────────────────────────────────────────────────
# On Vercel, set these in Project Settings → Environment Variables.
# NEXTAUTH_URL should be your Vercel deployment URL:
# e.g. https://your-app.vercel.app
# NEXT_PUBLIC_APP_URL should match NEXTAUTH_URL in production.
